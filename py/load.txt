python3.9 -m venv .venv
source .venv/bin/activate
pip install python-dotenv
python3 -m pip install -U discord.py
pip install beautifulsoup4
pip install requests


run all for needed venv packages ect.